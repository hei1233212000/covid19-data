{
  "name": "covid-19-data",
  "description": "Fetch the Covid-19 data and then upload the json file to a S3 bucket.",
  "version": "1.0.0",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.16.0",
    "node-fetch": "^2.6.1",
    "zlib": "^1.0.5"
  },
  "devDependencies": {
    "serverless-s3-remover": "^0.6.0"
  }
}
